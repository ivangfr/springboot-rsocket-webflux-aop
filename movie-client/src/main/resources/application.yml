spring:
  application:
    name: movie-client

app:
  movie-server:
    rsocket:
      host: localhost
      port: 7000

management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always
