spring:
  application:
    name: movie-client
  rsocket:
    server:
      port: 9082

server:
  port: 8082

app:
  movie-server:
    rsocket:
      port: 9081

management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always
