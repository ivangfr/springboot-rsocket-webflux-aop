spring:
  application:
    name: movie-server
  rsocket:
    server:
      transport: tcp
      port: 7000

management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always
